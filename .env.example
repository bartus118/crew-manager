# Konfiguracja Crew Manager
# Skopiuj ten plik na config.js i uzupełnij wartościami

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Admin Password (hasło do panelu administratora)
ADMIN_PASSWORD=your-secure-password

# Application Settings
APP_NAME=Crew Manager
APP_VERSION=1.0.0

# Database
# Tables used:
# - employees (id, first_name, last_name, bu, roles, permissions, created_at)
# - machines (number, status, created_at)
# - assignments (id, date, machine_number, role, employee_id, created_at)
# - vacation (id, employee_id, start_date, end_date, reason, created_at)
# - rdnst_workers (id, date, name, bu, employee_id, created_at)

# Note: employee_id is TEXT (not UUID) to support both regular employees and rdnst_XXXXX format
